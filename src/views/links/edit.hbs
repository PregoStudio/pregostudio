<div class="container">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card">
                <div class="card-body">
                    <form action="/links/edit/{{link.id}}" method="POST">
                        <div class="form-group m-2">
                            <input type="text" value="{{link.title}}" name="title" class="form-control"
                                placeholder="Title" autofocus>
                        </div>
                        <div class="form-group m-2">
                            <input type="url" value="{{link.url}}" name="url" class="form-control" placeholder="Url">
                        </div>
                        <div class="form-group m-2">
                            <textarea name="description" rows="10" class="form-control"
                                placeholder="Descripción">{{link.description}}</textarea>
                        </div>
                        <div class="form-group m-2">
                            <button class="btn btn-danger btn-block">
                                Guardar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>